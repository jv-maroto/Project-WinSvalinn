# WinSvalinn — runtime dependencies (Python sidecar)

# REST sidecar that exposes core/* to the Tauri React frontend
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.0

# System monitoring (CPU, RAM, processes, network)
psutil>=5.9.0

# Image processing (logos, icons, plugin previews)
pillow>=10.0.0

# YAML for CIS hardening profiles (plugin_hardening_cis)
PyYAML>=6.0

# Plugins still use CustomTkinter when launched in their own window
customtkinter>=5.2.0

# ─── Optional / dev ────────────────────────────────────────────────────
# Uncomment for development:
# pytest>=8.0
# pytest-cov>=4.0
# ruff>=0.4
# pyinstaller>=6.0
